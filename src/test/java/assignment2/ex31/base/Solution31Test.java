package assignment2.ex31.base;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class Solution31Test {

  /*
    @Test
    void DefaultTarget(){
        for (double i = 55; i <96 ; i++) {
            String actual = Solution31.targetHeartRate(22, 65, i/100);
            String expected = (int)(i) + "%          | " + (int)TargetHeartRate + " bpm";;
        }


    }
    @Test
    void DefaultTable(){
        Solution31.tableMaker();
    }*/

    //output of the functions are repeated lines containing ints within the strings. I couldn't figure out how to test
    //for only the ints within the strings and not the whole thing :(
}

